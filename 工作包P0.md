# 工作包：P0 — 基础与公共层（`common/` + RPC 配置与依赖注入）

## 🎯 总目标
为后续 `Check` 限流功能提供以下核心支撑：
- 统一错误原因字符串
- Redis Lua 滑动窗口原子逻辑
- 可选 L1 本地缓存
- RPC 可配置项与 `ServiceContext` 注入
- **约束**：`Logic` 层禁止直接创建 Redis 连接。

## ⚠️ 约束（必须遵守）
1. **错误码一致性**：业务原因字符串必须与 `pb.CheckResponse.reason` 及日志字段完全对应（例如：`RateLimitExceeded`）。
2. **原子性保障**：滑动窗口的「删窗外 + 计数 + 可能写入」必须在 **单段 Lua 脚本** 内完成，严禁拆分为多次 Redis 调用，以避免竞态条件（符合规范 3.1.2）。
3. **L1 缓存策略**：
   - 使用 `go-zero` 的 `collection.NewCache`。
   - TTL 极短（量级 ~500ms）。
   - 语义需与 L2 (Redis) key/窗口一致，严禁产生长期缓存导致与 Lua 逻辑矛盾。
4. **依赖注入原则**：Redis 客户端、L1 缓存、Lua 脚本句柄等资源 **仅** 在 `rpc/internal/svc` 初始化。`rpc/internal/logic` 包内 **禁止** 出现 `redis.NewClient` 或等价直连代码。

---

## 📦 产出物路径
- `common/errors/`（新建包，导出常量或类型）
- `common/algorithm/`（Lua 源码字符串或 `embed`，及必要的 Go 封装）
- `common/cache/`（L1 封装，或薄封装 `collection.NewCache`）
- `rpc/internal/config/config.go` + `rpc/etc/sentinel.yaml`
- `rpc/internal/svc/service_context.go`

---

## 🛠️ 任务详情

### 任务 1 — `common/errors`
**输入**：现有 `rpc/pb` 中 `CheckResponse.reason` 为 `string`。

**要求**：
1. 新增包：`SentinelGrain/common/errors`（或项目实际 module 名 + `common/errors`）。
2. 导出限流相关常量（或带 `Code() string` 的类型），至少包含：
   - `RateLimitExceeded`
   - （若 MVP 需要）`InternalError`, `RuleNotFound` 等（需与产品定义一致）。
3. 常量值即为 gRPC 响应 `reason` 与结构化日志中使用的同一字面量。

**✅ 验收标准**：
- `go build ./...` 通过。
- 其他包可 `import ".../common/errors"` 并引用这些常量。
- 代码中无魔法字符串散落在 `logic` 中（后续 `Check` 实现时强制使用常量）。

---

### 任务 2 — `common/algorithm`（Redis ZSET 滑动窗口 + Lua）
**输入**：规范要求使用 ZSET；`ZREMRANGEBYSCORE` 移除窗口外；窗口内计数与阈值比较；需要原子性。

**要求**：
1. **Lua 脚本实现**：
   - 形式：字符串常量或 `//go:embed` 文件。
   - **逻辑顺序固定**：
     1. 按窗口下界 `ZREMRANGEBYSCORE` 移除过期数据。
     2. `ZCARD`（或等价）得到窗口内当前计数。
     3. 与传入阈值比较；若允许则执行 `ZADD`。
        - *注*：`member` 唯一性可用 `时间戳+随机数` 或 `Redis 自增`，避免冲突。
     4. 返回结构化结果给 Go（例如：`allowed`, `current_count`, `remaining`），字段名需与 Go 解析结构体一致。
2. **Go 侧封装**：
   - 提供最小化函数，例如：
     ```go
     EvalSlideWindow(ctx context.Context, redis *redis.Redis, key string, nowMs, windowMs, threshold, cost int64) (allowed bool, remaining int64, err error)
     ```
   - 支持 `cost` 参数（一次扣多格）。
3. **优化（可选）**：脚本 SHA 预加载。可在 `svc` 启动时 `ScriptLoad`，封装层接受 `*redis.Script` 或预计算的 SHA。

**✅ 验收标准**：
- 单元测试（使用 `miniredis`）或集成测试（连真实 Redis）验证：
  - 窗口外请求不计入。
  - 超限拒绝。
  - `cost` 行为符合设计。
- **严禁** 出现「先 `ZCARD` 再 `ZADD`」拆成两次非 Lua 调用的主路径代码。

---

### 任务 3 — `common/cache`（L1）
**要求**：
1. 使用 `github.com/zeromicro/go-zero/core/collection.NewCache`（或项目已锁定版本中的等价 API）。
2. **封装构造**：
   - 函数签名：`NewL1(ttl time.Duration)`。
   - 暴露接口：`Get/Set` 或针对「Check 结果/中间态」的专用接口。
   - **文档注释**：必须明确说明缓存的是哪类 key（与 Redis 限流 key 的对应关系）、默认 TTL (~500ms)。
3. **一致性警告**：禁止在 L1 中长期缓存「与 Redis 窗口计数不一致会误导放行」的数据。若缓存的是「最近一次 Check 结果」，须在文档中写清这是与规范的折中方案。

**✅ 验收标准**：
- `rpc/internal/svc` 可一行创建并注入。
- TTL 支持从配置读取（见任务 4）。

---

### 任务 4 — RPC config 扩展 + `sentinel.yaml`
**要求**：
1. **`rpc/internal/config.Config` 扩展**：
   增加字段（命名与 go-zero yaml 标签一致），至少包括：
   - **Redis**：`Host`, `Pass`, `Type`, `Tls` 等（与 `go-zero redis.RedisConf` 对齐或内嵌）。
   - **超时控制**：Redis 操作专用超时（如 `CommandTimeout`，区别于 `SlowThreshold`）。
   - **L1**：`Enabled` (bool), `TTL` (毫秒或 duration 字符串)。
   - **指标**：`MetricsEnabled` (bool) 或 Prometheus 子结构（与 go-zero 惯例一致）。
2. **`rpc/etc/sentinel.yaml` 示例**：
   - 给出本地可运行示例值。
   - **服务发现处理**：若保留 Etcd，需明确说明本地单机开发模式下如何关闭服务发现（例如：`Mode: dev`、注释 Etcd 配置、或使用官方推荐的本地模式写法），确保执行 `go run ./rpc/cmd/sentinel-rpc -f rpc/etc/sentinel.yaml` 时不依赖未启动的 Etcd。

**✅ 验收标准**：
- 加载 yaml 后结构体字段非零默认值符合预期。
- 缺省项有合理默认值（可在代码中通过 `fillDefault` 实现）。

---

### 任务 5 — `rpc/internal/svc` 依赖注入
**要求**：
1. **`ServiceContext` 结构体字段**：
   - `Config`
   - `BizRedis` (`*redis.Redis`)
   - `L1Cache` (`*collection.Cache` 或任务 3 的封装类型)
   - `LimitScript` (`*redis.Script` 或 脚本 SHA 字符串 + 获取方法，若使用预加载)
2. **`NewServiceContext(c config.Config)` 实现**：
   - 根据 `c` 创建 Redis 客户端。
   - 按配置创建 L1（若未启用，`L1` 设为 `nil`，后续 logic 需做 nil 判断）。
   - 可选：加载 Lua 脚本。
3. **错误处理**：若 Redis 必填项缺失，在启动期 `panic` 或返回 `error`（风格需与项目现有 `MustNew` 保持一致）。

**✅ 验收标准**：
- `rpc/internal/logic` 中仍 **无** Redis 直连代码；仅通过 `l.svcCtx.BizRedis` 等方式访问。
- `go build ./rpc/...` 通过。

---

## 🔄 Agent 执行顺序建议
推荐顺序：**1 → 2 → 3 → 4 → 5**
- **5** 依赖 **4** 的配置结构与 **3** 的构造方式。
- **2** 可先于 **3** 独立开发。
- **1** 可与 **2** 并行开发。

## 🏁 完成定义（整包 Done）
- [ ] 上述路径文件全部存在且可编译 (`go build ./...`)。
- [ ] Lua 滑动窗口逻辑满足原子性要求。
- [ ] L1 缓存可配置且默认 TTL ≈ 500ms。
- [ ] RPC 配置文件包含 Redis、超时、L1、指标开关等完整项。
- [ ] `ServiceContext` 完成所有依赖注入，且 `Logic` 层无任何新建连接操作。