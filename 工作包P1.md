# 任务：Check 核心链路（性能与健壮性）

## 🎯 目标
在 **go-zero zRPC** 服务中完整实现 `Check` 业务逻辑：
1.  **维度构建**：按与配额规则一致的维度拼接 Key。
2.  **缓存策略**：执行“先 L1 后 Redis Lua”的两级判定流程。
3.  **配置读取**：从内存阈值表读取 `threshold` 和 `period`。
4.  **规范落地**：
    *   落实 **规范 3.3** 的性能细节（对象复用、超时控制、异步安全）。
    *   落实 **规范 4.1** 的结构化日志。
    *   落实 **规范 4.2** 的 Prometheus 指标，并在服务中暴露 metrics 端口。

---

## ⚠️ 约束与依据
*   **目录与依赖注入**：
    *   代码位于 `rpc/internal/logic`。
    *   依赖从 `rpc/internal/svc/service_context.go` 注入。
    *   **严禁**：Logic 层禁止自建 Redis 连接，必须从 `svcCtx` 获取。（参考 `开发规范.md` 第 1、3.4 节）
*   **原子性要求**：
    *   L2 限流逻辑必须在 **Redis Lua** 中原子完成。（参考 `开发规范.md` 第 3.1、3.2 节）
*   **性能规范**：
    *   使用 `sync.Pool` 复用临时对象。
    *   所有 Redis 调用必须包裹 `context.WithTimeout`。
    *   非关键路径异步任务使用 `threading.GoSafe`。（参考 `开发规范.md` 第 3.3 节）
*   **日志规范**：
    *   使用 `logx.WithContext(ctx).Infow(...)`。（参考 `开发规范.md` 第 4.1 节）
*   **指标规范**：
    *   指标名称与含义需严格对应。（参考 `开发规范.md` 第 4.2 节）

---

## ✅ 必须实现的行为

### A. 维度 Key 构建
*   **定义**：定义与配额规则维度完全一致的 Key 拼接逻辑。
*   **格式**：`app_id` + `resource` + `dimension`。
*   **要求**：具体分隔符或规范化方式需在代码中与规则表保持一致，并在注释或常量中清晰定义，避免歧义。

### B. 判定流程
1.  **先查 L1**：
    *   若命中，立即返回结果。
    *   **动作**：递增/记录 `sentinel_l1_hit_total`（或等价观测方式，确保语义一致）。
2.  **未命中走 L2 (Redis Lua)**：
    *   执行滑动窗口 ZSET 流程（需符合 `开发规范.md` 3.2）。
    *   从 **内存阈值表** 读取该 Key（或规则匹配结果）对应的 `threshold` 和 `period` (秒)。
3.  **无匹配规则时的 MVP 行为**（三选一，需在任务/PR 中明确选定项并代码实现一致）：
    *   [ ] 默认放行
    *   [x] 默认拒绝
    *   [ ] 使用全局默认阈值（需在配置或常量中可辨识）

### C. 性能与健壮性（规范 3.3）
*   **内存复用**：对 Check 热路径上频繁分配的临时对象，使用 `sync.Pool` 进行复用。
*   **超时控制**：所有 Redis 调用必须使用 `context.WithTimeout`（超时时间需与配置一致或可配置）。
*   **并发安全**：指标上报、日志中非关键路径若异步执行，必须使用 `threading.GoSafe` 包装，**禁止**使用裸 `go` 启动协程。

### D. 结构化日志（规范 4.1）
*   **方法**：使用 `logx.WithContext(ctx).Infow(...)`。
*   **必填字段**：
    *   `app_id`
    *   `allowed`
    *   `latency_ms`
*   **选填字段**（便于排障）：`resource`, `dimension`, `reason` 等。

### E. Prometheus 指标（规范 4.2）
*   **注册并使用以下指标**（名称与类型需匹配语义）：
    *   `sentinel_request_total` (Counter): 每次 Check 计数。
    *   `sentinel_reject_total` (Counter): `allowed == false` 时递增。
    *   `sentinel_l1_hit_total` (Counter): L1 命中时递增。
    *   `sentinel_check_latency_seconds` (Histogram): 观测整次 Check 延迟。
*   **端口暴露**：
    *   在 go-zero 服务中暴露 **metrics HTTP 端口**。
    *   配置需与 `zrpc` 所用配置一致（与 `rpc/etc` 中 Prometheus/Metrics 相关配置对齐），避免端口或路径漂移。

---

## 🧪 验收标准（Agent 自测清单）

- [ ] **测试覆盖**：单测或集成测试覆盖以下路径：
    - L1 命中路径。
    - L1 未命中走 Lua 路径。
    - 无规则时的 MVP 行为路径。
- [ ] **超时健壮性**：模拟 Redis 慢响应或超时时，请求能在预期时间内返回或进入可预期的错误路径，**绝不无限阻塞**（验证 `WithTimeout` 有效性）。
- [ ] **监控可观测**：服务启动后，Prometheus 可 scrape 到上述四个指标系列（或在文档中明确说明 scrape 路径与端口）。
- [ ] **日志可检索**：日志系统中可通过上下文字段检索，必须包含 `app_id`、`allowed`、`latency_ms`。

---

## 📦 交付物说明（给 Agent）

1.  **文件范围**：修改/新增的文件应限于：
    *   Check Logic 实现
    *   Lua/Redis 封装调用
    *   ServiceContext 注入扩展
    *   指标注册代码
    *   配置文件（若涉及全局默认值或 MVP 开关）
2.  **提交规范**：
    *   在 PR 描述或提交说明中，**必须明确写出**：
        *   无规则时的 **MVP 选项**（放行 / 拒绝 / 全局默认）。
        *   若适用，全局默认阈值的 **来源**（配置项或常量）。
