# SentinelGrain 项目规范文档

## 1. 项目目录结构规范
遵循 **go-zero** 的最佳实践，并针对“两级缓存”和“插件化限流”进行目录扩展。

```text
SentinelGrain/
├── api/                # HTTP 接口定义 (Optional, 用于管理后台控制配额)
├── rpc/                # 核心逻辑：gRPC 接口定义与实现
│   ├── cmd/
│   │   └── sentinel-rpc/  # gRPC 服务入口 (main，位于 rpc 下以便导入 rpc/internal)
│   ├── pb/             # Protobuf 生成文件 (import: SentinelGrain/rpc/pb)
│   ├── client/         # zRPC 客户端封装（供其他服务调用）
│   ├── internal/
│   │   ├── config/     # 配置文件解析
│   │   ├── logic/      # 核心业务：滑动窗口判定、两级缓存置换逻辑
│   │   ├── server/     # gRPC Server 包装
│   │   └── svc/        # 依赖注入 (Redis, L1 Cache, Prometheus)
│   ├── etc/            # 服务配置 (如 sentinel.yaml)
│   └── sentinel.proto  # 定义 Check 接口
├── common/             # 公共组件
│   ├── algorithm/      # 滑动窗口、令牌桶算法实现
│   ├── cache/          # L1 (FreeCache/BigCache) 封装
│   └── errors/         # 业务错误码定义
├── deploy/             # 云原生部署
│   ├── docker/         # Dockerfile
│   └── k8s/            # Deployment & Service (含 Prometheus Annotations)
└── go.mod

```

---

## 2. gRPC 接口设计规范 (Protobuf)
为了追求 **5ms 以内** 的延迟，Protobuf 的设计应保持扁平化，避免复杂的嵌套。

```protobuf
syntax = "proto3";

package sentinel;
option go_package = "SentinelGrain/rpc/pb;pb";

message CheckRequest {
    string app_id = 1;
    string resource = 2;    // API Path 或 Service Name
    string dimension = 3;   // 用户ID、IP 等组合 Key
    int64  cost = 4;        // 本次消耗的配额度
}

message CheckResponse {
    bool   allowed = 1;     // 是否放行
    int64  remaining = 2;   // 剩余配额
    string reason = 3;      // 拒绝原因（如：RateLimitExceeded）
}

service Sentinel {
    rpc Check(CheckRequest) returns (CheckResponse);
}
```

**生成 Go 代码**（在仓库根目录执行，需已安装 `protoc`、`protoc-gen-go`、`protoc-gen-go-grpc`）：

```bash
protoc --proto_path=. --go_out=. --go_opt=module=SentinelGrain --go-grpc_out=. --go-grpc_opt=module=SentinelGrain rpc/sentinel.proto
```

若本机已安装 **goctl**，也可用 `goctl rpc protoc` 按团队既有参数生成；勿手工改 `rpc/pb/*.pb.go` 中与 proto 不一致的内容。

**启动 RPC 服务**：在 `rpc` 目录下 `go run ./cmd/sentinel-rpc`（默认读取 `etc/sentinel.yaml`）；若在仓库根目录执行，使用 `go run ./rpc/cmd/sentinel-rpc -f rpc/etc/sentinel.yaml`。

---

## 3. 核心开发准则

### 3.1 两级缓存 (L1/L2) 实现策略
这是支撑每秒数万次请求的关键。

### 3.1 两级缓存 (L1/L2) 实现策略

这是支撑每秒数万次请求的关键。

*   **L1 (Local)**: 优先使用 go-zero 内置的 `collection.NewCache(duration)`。
    *   存储高频访问的 `hot-keys`。
    *   缓存时间极短（如 500ms），牺牲微弱的全局一致性换取极高的判定性能。

	**代码示例注入：**

```go
// svc/service_context.go 中定义
type ServiceContext struct {
    Config    config.Config
    L1Cache   *collection.Cache
    BizRedis  *redis.Redis
}
```

*   **L2 (Global)**: 使用 **Redis**。
    *   限流逻辑必须封装在 **Lua 脚本** 中运行，确保 `Read-Check-Update` 操作的原子性。



### 3.2 滑动窗口算法
不要使用简单的 `INCR`。在 Redis 中利用 **ZSET** 实现滑动窗口：

*   **Key**: 维度组合。
*   **Member**: 当前微秒时间戳（或 UUID）。
*   **Score**: 时间戳。
*   **逻辑**:
    1.  `ZREMRANGEBYSCORE` 移除窗口外数据。
    2.  `ZCARD` 统计窗口内总数。
    3.  判断并 `ZADD`。

### 3.3 高性能编程规范
*   **内存复用**: 针对频繁的 `CheckRequest` 处理，使用 `sync.Pool` 复用临时对象，减轻 GC 压力。
*   **并发控制**: 使用 `threading.GoSafe` 启动后台异步任务（如 Metrics 上报），防止 Goroutine 泄露导致系统崩溃。
*   **超时控制**: 所有的 Redis 操作必须带上 `context.WithTimeout`，确保 L2 抖动时不阻塞整个判定链路。

## 3.4 资源注入规范

所有外部依赖（Redis 客户端、L1 Cache 实例、自定义 Algorithm 实例）必须在 `svc/service_context.go` 的 `ServiceContext` 结构体中初始化。

Logic 层禁止自行创建数据库或 Redis 连接，必须从 `l.svcCtx` 中获取。


---

## 4. 云原生与监控规范

### 4.1 结构化日志 (zap)
日志必须包含上下文 `TraceID`（go-zero 自动集成），以便在微服务链路中追踪。

```go
logx.WithContext(ctx).Infow("Quota Check", 
    "app_id", req.AppId, 
    "allowed", resp.Allowed, 
    "latency_ms", duration)
```

### 4.2 Prometheus 指标
必须暴露以下核心指标：

*   `sentinel_request_total`: 总请求数。
*   `sentinel_reject_total`: 被限流拒绝的请求数。
*   `sentinel_l1_hit_total`: L1 本地缓存命中数（体现两级缓存效率）。
*   `sentinel_check_latency_seconds`: 判定耗时分布（Histogram）。

### 4.3 Kubernetes 适配
在 `deployment.yaml` 中配置 `resources.limits`，并利用 go-zero 的 K8s Probes：

```yaml
livenessProbe:
  tcpSocket:
    port: 8080
readinessProbe:
  tcpSocket:
    port: 8080
```

---

## 5. 动态配额配置规范
利用 **go-zero** 的 `config.Config` 热加载能力，或者对接 **Apollo/Nacos**。

*   **实现方式**: 监听配置中心变更，实时更新内存中的限流阈值映射表（Threshold Map）。
*   **目标**: 确保无需重启 Pod 即可调整 VIP 用户的配额。
